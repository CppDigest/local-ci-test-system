name: Integration Success
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: "Hello"
            runs-on: ubuntu-latest
            compiler: gcc
            version: "15"
    steps:
      - run: echo "Hello"
